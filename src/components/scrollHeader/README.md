A scroll header is placed on a material-toolbar using the `scroll-header` attribute. 

It will cause the toolbar to hide itself as the user scrolls down, and show itself again when the user scrolls up.
